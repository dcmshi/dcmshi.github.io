# https://dcmshi.github.io/
#
# Nothing here is private, so everything is crawlable. 404.html stays out of
# the index via its own noindex meta tag rather than a Disallow here — a
# Disallow would stop crawlers fetching the page and so stop them ever reading
# that tag.

User-agent: *
Allow: /

Sitemap: https://dcmshi.github.io/sitemap.xml
