<?xml version="1.0" encoding="UTF-8"?>
<!--
  One page, one entry. The in-page sections (#about, #projects, #contact) are
  fragments of this same document, not separate URLs, so they don't belong here.
  404.html is noindex and is likewise omitted.

  changefreq, priority and lastmod are all deliberately absent. Google ignores
  the first two, and priority is meaningless in a single-URL sitemap. lastmod
  would have to be updated by hand on every content change, and a stale date is
  a worse signal than no date — if it's ever wanted, generate it from the commit
  date in CI rather than typing it in.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>https://dcmshi.github.io/</loc>
    </url>
</urlset>
